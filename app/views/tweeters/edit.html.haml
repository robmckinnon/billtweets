%h1 Editing tweeter

= error_messages_for :tweeter

- form_for(:tweeter, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url