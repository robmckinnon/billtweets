%h1 Editing tweeter

= error_messages_for :tweeter

- bits = object_url.split('//').last.split('/')
- form_for(:tweeter, :url => "http://#{bits[0]}/tweeters/#{@object.id}", :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"

= link_to 'Show', object_url
|
= link_to 'Back', collection_url