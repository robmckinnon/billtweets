%h1 Editing news_item

= error_messages_for :news_item

- form_for(:news_item, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Update"
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url