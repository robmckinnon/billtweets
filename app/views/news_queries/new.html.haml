%h1 New news_query

= error_messages_for :news_query

- form_for(:news_query, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"

= link_to 'Back', collection_url